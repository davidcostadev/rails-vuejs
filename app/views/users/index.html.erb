<h1>Users</h1>

<ul>
    <% @users.each do |user| %>
        <li><strong><%= link_to user.name, user %></strong> - <%= user.email %></li>
    <% end %>
</ul>

<% if @users.length == 0 %>
    <p>No one user finded</p>
<% end %>
